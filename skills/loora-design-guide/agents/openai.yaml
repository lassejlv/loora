interface:
  display_name: "Loora Design Guide"
  short_description: "Build schema-safe structured designs in Loora"
  default_prompt: "Use $loora-design-guide to build and verify a schema-safe structured design in Loora."
dependencies:
  tools:
    - type: "mcp"
      value: "loora"
      description: "Loora structured design and Canvas MCP server"
      transport: "streamable_http"
      url: "https://mcp.loora.design/mcp"
