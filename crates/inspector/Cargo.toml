[package]
name = "loora-inspector"
version.workspace = true
edition.workspace = true

[dependencies]
gpui.workspace = true
loora-ui.workspace = true
serde_json = "1"

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }
