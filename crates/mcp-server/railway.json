{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "crates/mcp-server/Dockerfile",
    "watchPatterns": [
      "crates/mcp-server/**",
      "Cargo.toml",
      "Cargo.lock"
    ]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "sleepApplication": false,
    "healthcheckPath": "/health"
  }
}
